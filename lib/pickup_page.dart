import 'package:flutter/material.dart';

class PickupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pickup Page'),
      ),
      body: Center(
        child: Text('Welcome to the Pickup Page!'),
      ),
    );
  }
}
